require "spec_helper"

describe Numverify do
  it "has a version number" do
    expect(Numverify::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
